<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CoursePortal</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fc;
      font-family: 'Segoe UI', sans-serif;
    }

    .navbar {
      background-color: #ffffff;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      padding: 80px 20px;
      max-width: 1200px;
      margin: auto;
    }

    .hero-text {
      max-width: 600px;
    }

    .hero-text h1 {
      font-size: 2.8rem;
      font-weight: 600;
      color: #333;
    }

    .hero-text p {
      font-size: 1.2rem;
      color: #666;
      margin-top: 15px;
    }

    .hero-buttons {
      margin-top: 30px;
    }

    .hero-buttons .btn {
      padding: 12px 24px;
      font-size: 1rem;
      border-radius: 8px;
    }

    .hero-graphic {
      flex: 1;
      min-width: 300px;
      max-width: 400px;
      height: 300px;
      background-color: #e3f2fd;
      border-radius: 20px;
      position: relative;
    }

    .hero-graphic::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 180px;
      height: 80px;
      background-color: #bbdefb;
      border-radius: 40px;
      transform: translate(-50%, -50%);
    }

    footer {
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: #888;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand fw-bold text-dark" href="/">CoursePortal</a>
      <div class="collapse navbar-collapse justify-content-end">
        <ul class="navbar-nav me-3">
          <li class="nav-item"><a class="nav-link text-dark" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link text-dark" href="/courses">Courses</a></li>
          <li class="nav-item"><a class="nav-link text-dark" href="#">Contact</a></li>
        </ul>
        <% if (user) { %>
          <a href="/dashboard" class="btn btn-outline-primary me-2">Dashboard</a>
          <a href="/logout" class="btn btn-primary">Logout</a>
        <% } else { %>
          <a href="/login" class="btn btn-outline-primary me-2">Login</a>
          <a href="/register" class="btn btn-primary">Register</a>
        <% } %>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-text">
      <h1>Welcome to Your Learning Hub</h1>
      <p>Explore curated courses, track your progress, and grow your skills — all in one place.</p>
      <div class="hero-buttons">
        <a href="/register" class="btn btn-primary me-3">Get Started</a>
        <a href="/courses" class="btn btn-outline-primary">Browse Courses</a>
      </div>
    </div>
    <div class="hero-graphic"></div>
  </section>

  <!-- Footer -->
  <footer>
    &copy; 2025 CoursePortal. All rights reserved.
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>